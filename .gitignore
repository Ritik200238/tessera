# Node
node_modules/
.pnpm-store/
.npm/
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artifacts
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo

# Rust / Cargo
target/
**/target/
Cargo.lock.bak

# Foundry
contracts/solidity/cache/
contracts/solidity/out/
contracts/solidity/broadcast/
contracts/solidity/.gas-snapshot

# Env & secrets — ignore ALL dotenv files EXCEPT *.example templates
.env
.env.*
!.env.example
!.env.*.example

# Local data
.data/
*.sqlite
*.sqlite-journal
*.db

# Logs
logs/
*.log

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Coverage
coverage/
lcov.info

# Local reference clones (source-of-truth docs, reproducible upstream — not part of the product)
# Anchored to repo root so it does NOT match the product's docs route (web/app/docs).
/docs/

# Foundry installed dependencies (restore with `forge install`)
contracts/solidity/lib/

# Vercel CLI link
.vercel

# Stylus redeploy artifacts (scripts/redeploy-vault.ps1)
/vault-opt.wasm
/vault-build.log
/vault-deploy.log

# E2E / QA transient artifacts (Playwright internals + ad-hoc MCP captures).
# The curated audit evidence in qa-evidence/ IS committed; these are not.
web/e2e/.artifacts/
web/test-results/
web/playwright-report/
/.playwright-mcp/
/preflight-landing.png

# Local secret paste-file for the Render drill rig
/render-drill-env.txt

# MCP server is standalone (its own npm install)
mcp/node_modules/

# --- Internal / working docs (kept locally, not in the public repo) ---
/CLAUDE.md
/enhancedVersion.md
/front-end design.md
/buildathon/
/qa-evidence/
# Scratch / analysis artifacts
/UPDATE10.md
/research.md
/arb_dex.json
/arb_fees.json
/arb_tvl.json
/.claude/
# Secrets (belt-and-braces — never commit)
.env.testnet
render-drill-env.txt
vault-opt.wasm
vault-opt2.wasm
