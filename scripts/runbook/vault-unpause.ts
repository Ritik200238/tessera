#!/usr/bin/env tsx
/**
 * Runbook: unpause the vault (TDD §16.3).
 *
 * ⚠️ PHASE 2 — DEFERRED.
 *
 * Will call `TesseraVault.unpause()` from the owner key once the Stylus vault is deployed.
 * Deliberate stub: the contract surface is owned by Agent B (Phase 2).
 */
console.error(
  "[runbook:vault:unpause] not yet implemented — TesseraVault.unpause() ships with the Phase 2 Stylus vault.",
);
process.exit(2);
