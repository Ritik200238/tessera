#!/usr/bin/env tsx
/**
 * Runbook: pause the vault (TDD §16.3).
 *
 * ⚠️ PHASE 2 — DEFERRED.
 *
 * Will call `TesseraVault.pause()` from the owner key once the Stylus vault is deployed.
 * Deliberate stub: the contract surface is owned by Agent B (Phase 2).
 */
console.error(
  "[runbook:vault:pause] not yet implemented — TesseraVault.pause() ships with the Phase 2 Stylus vault.",
);
process.exit(2);
